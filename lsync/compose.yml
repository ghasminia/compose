services:
  lsyncd:
    image: ghaseminia/lsync:1.0
    container_name: lsyncd
    volumes:
      - /root/projects/web-php/src:/source
      - ./src:/target
      - ./lsyncd.conf.lua:/etc/lsyncd/lsyncd.conf.lua
    restart: always
